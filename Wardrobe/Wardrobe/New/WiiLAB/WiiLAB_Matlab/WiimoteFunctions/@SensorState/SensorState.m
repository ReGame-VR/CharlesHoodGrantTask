% SensorState is the class that creates the data structure for storing the
% SensorState values from the Wiimote (Balance Board)

classdef SensorState
    
    properties 
        
        BottomLeft = 0;
        BottomRight = 0;
        TopLeft = 0;
        TopRight = 0;
        
    end % properties
    
end
    